amd64 python

```xml
<use src="{template}/windows/amd64/python" />
```

amd64 pip

```xml
<use src="{template}/windows/amd64/pip" />
```

amd64 ALL

```xml
<use src="{template}/windows/amd64" />
```

arm64 python

```xml
<use src="{template}/windows/arm64/python" />
```

arm64 pip

```xml
<use src="{template}/windows/arm64/pip" />
```

arm64 ALL

```xml
<use src="{template}/windows/arm64" />
```
